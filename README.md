# rock-paper-scissors
play game
1.imported a random module
2.store the choices in a list
3.explaining user what are the choices
4.write a function for if a player choice is rock
   .declare variables as global as we can use outside of the function
   .use else if ladder with condition of bots choice
5.write a function for if a player choice is paper 
   .declare player score and bot score as global again because we are updating the values
   .use the  else if ladder with conditions of bots choice
6.write a function for if a player choice is scissors
   .declare player score and bot score as global again because we are updating the values
   .use the  else if ladder with conditions of bots choice
7.use a while loop so the game will be stoped when the player wants to stop
   .take the input from the user and use random module for taking random choice frome the    user
   .use else if ladder to enter into the function based on the users choice 
   .use break statement if a players choice is 999 then it stops the game
   .if player choses any other choice should say to print the write option
8.print the final scores after player stopes the game 
9.display the winner
   . if both have same score then say both have same scores
10.print thank you message for playing this game.

